# Engineering Implementation Prompt: GPS Broadcast Path & Ledger Depth Flywheel

## Context & Reference
Growth plan: @/sec/root/SHFTCCONK/skene-context/growth-plan.md

## What We're Building
Implement the GPS broadcast path and ledger depth flywheel at 92% confidence. This covers:
- Zero-auth driver PWA (sub-200KB, 3G-tolerant, IndexedDB queue, cell tower fallback)
- V/T ratio instrumentation in PostHog with per-vehicle GPS session coverage tracking
- Upstash Redis delayed SMS nudges at Hour-4 and Hour-24 keyed to absence of first GPS ping
- Hyperledger Fabric event writer binding every GPS ping to a ledger transaction
- Compliance-framed free tier gate with 30-day ledger display cap and loss-aversion upgrade prompt

## Technical Tasks to Implement

Provide step-by-step implementation with working code for each task below:

### Task 1: Driver PWA Scaffold
- Path: `/apps/driver-pwa`
- SvelteKit static adapter
- Geolocation API with IndexedDB queue that flushes on reconnect
- Cell tower fallback flag when accuracy < 50m
- Must load in < 4s on Safaricom 3G, bundle < 200KB

### Task 2: GPS Ping Ingest Endpoint
- Path: `src/routes/api/gps/ping/+server.ts`
- Validate device token
- Write to PostgreSQL `trip_events` table
- Enqueue Hyperledger Fabric write via job queue
- Zero ping loss tolerance — IndexedDB queue flush must confirm delivery

### Task 3: Hyperledger Fabric Event Writer
- Path: `src/lib/server/fabric/writeTripEvent.ts`
- Map GPS ping payload to ledger transaction
- Stamp with `vehicle_id`, `route_corridor`, `timestamp`, `accuracy_flag`
- Target > 99.5% write success rate per ping ingested

### Task 4: V/T Ratio Computation
- Path: `src/lib/server/analytics/vehicleCoverage.ts`
- DuckDB query over `trip_events` per vehicle per day against `expected_trip_windows`
- Emit PostHog event `vehicle_vt_ratio_updated` on each compute cycle
- Flag any vehicle with V/T ratio < 0.5 for operator 6AM WhatsApp briefing

### Task 5: GPS Activation Nudge Jobs
- Path: `src/lib/server/jobs/gpsActivationNudge.ts`
- Upstash Redis delayed jobs enqueued at account signup
- Fire SMS at T+4h if no ping received for org
- Fire compliance-framed SMS at T+24h if still no ping
- Cancel both jobs on first GPS ping received

### Task 6: Free Tier Ledger Gate UI
- Path: `src/lib/components/FreeTierLedgerGate.svelte`
- Query `ledger_event_count` and `days_until_cap`
- Render loss-aversion prompt showing trip event count and KES compliance protection estimate
- Surface at Day 28 of operator ledger retention
- Target > 15% upgrade conversion on prompt display

### Task 7: STK Push Upgrade Flow
- Path: `src/routes/api/billing/mpesa-upgrade/+server.ts`
- Pre-populate with ledger stats from gate component
- Fire Daraja STK push
- Handle idempotency via Upstash Redis key on `operator_id + plan_id`

## Data Triggers to Wire
| Trigger | Action |
|---|---|
| No GPS ping after 4h post-signup | Fire Hour-4 SMS nudge |
| No GPS ping after 24h post-signup | Fire Hour-24 compliance SMS |
| First GPS ping received | Cancel nudge jobs, unlock full nav, trigger UI celebration, write ledger genesis event |
| Operator ledger retention hits Day 28 | Surface `FreeTierLedgerGate` upgrade prompt |
| Fleet active vehicles ≥ 5 AND ledger history ≥ 60 days | Unlock Fleet Intelligence gate UI |
| V/T ratio < 0.5 for any vehicle | Flag in 6AM WhatsApp briefing |

## Success Metrics to Satisfy
- Time to First GPS Ping < 48h for > 60% of new operator accounts
- V/T ratio > 0.75 across active fleet at Month 2
- Driver PWA load < 4s on Safaricom 3G
- Zero GPS ping loss from connectivity drops
- Free-tier ledger gate upgrade conversion > 15%
- Hyperledger write success > 99.5% per ping ingested

## Instructions
1. Implement each task in the order listed above
2. Provide complete, production-ready code for every file — no stubs or placeholders
3. Show the exact data flow between components (PWA → ingest endpoint → Fabric writer → PostHog)
4. Include error handling, retry logic, and idempotency where specified
5. Call out any schema migrations needed for `trip_events` or related PostgreSQL tables
6. Flag any environment variables or secrets required per task
7. After all tasks, provide a smoke-test checklist to verify the full GPS ping path end-to-end